// .spec.md §4.3 — a straight accessibility gap, not a nice-to-have. Chakra
// v3 already ships SkipNavLink/SkipNavContent with exactly the required
// behaviour (visually hidden until focus, then pinned top-left) — no
// custom recipe needed.
export { SkipNavLink, SkipNavContent } from "@chakra-ui/react";
